import { Link } from "react-router-dom";

const Unstake = () => {
    return (
        <div>
            <h1>Unstake</h1>
            <Link to="/dashboard">Dashboard</Link>
        </div>
    );
}

export default Unstake;
