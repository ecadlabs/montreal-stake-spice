import { Link } from "react-router-dom";

const Connect = () => {
    return (
        <div>
        <h1>Connect</h1>
        <Link to="/dashboard">Dashboard</Link>
        </div>
    );
}

export default Connect;
