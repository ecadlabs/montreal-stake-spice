import { Link } from "react-router-dom";

const Stake = () => {
    return (
        <div>
            <h1>Stake</h1>
            <Link to="/dashboard">Dashboard</Link>
        </div>
    );
}

export default Stake;
