import { Link } from "react-router-dom";

const Delegate = () => {
    return (
        <div>
            <h1>Delegate</h1>
            <Link to="/dashboard">Dashboard</Link>
        </div>
    );
}

export default Delegate;